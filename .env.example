DATABASE_URL=postgresql://postgres:postgres@localhost:5432/boardgames

# Optional: Secret used to sign management session cookies.
# If not set, a random secret is generated on each startup (sessions won't survive restarts).
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=
